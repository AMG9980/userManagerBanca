{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="full-screen-container" style=" margin-left: 300px;margin-right: 30px;">
        <div class="full-screen-container">
            <h1 class="bold-red-text">User edit</h1>
        </div>
        <h1 class="bold-red-text">User edit</h1>


    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <br>
    <form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" style="display:inline-block" onsubmit="return confirm('Êtes vous vraiment sûr ?')">
        <a href="{{ path('app_user_index') }}" class="btn btn-success btn-lg rounded-pill"><i class="bi bi-arrow-return-left " aria-hidden="true"></i>back to list</a>
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
        <button class="btn btn-danger btn-lg"><i class="bi bi-trash " aria-hidden="true"></i> Delete</button>
    </form>
    </div>
{% endblock %}
